id = "acp"
name = "Agent Client Protocol"
version = "0.1.0"
abi = 2
description = "Integrates ACP (Agent Client Protocol) for AI-assisted editing"

# Built library (release)
library_path = "libtome_acp_plugin.so"

# Dev path for cargo watch workflows (debug build, workspace target dir)
dev_library_path = "../../target/debug/libtome_acp_plugin.so"
